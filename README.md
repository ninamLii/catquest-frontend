# Cat Quest Readme

## Getting Started

1. Install dependencies with npm install
2. Create .env and add the env variables. An example is in env.template
3. For local testing use an CORS plugin. This is necessary if you run the backend and frontend locally